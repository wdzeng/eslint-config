// Please also update JS/TS test if you are changing this file.

import { describe, expect, test } from 'vitest'

describe('foo', () => {
  test('bar', () => {
    expect(1).toBe(1)
  })
})

describe('doo', () => {
  test('dar', () => {
    expect(1).toBe(1)
  })
})
