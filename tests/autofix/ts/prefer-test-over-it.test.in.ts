// Please also update JS/TS test if you are changing this file.

import { describe, expect, it } from 'vitest'

it('foo', () => {
  expect(1).toBe(1)
})

describe('bar', () => {
  it('baz', () => {
    expect(1).toBe(1)
  })
})
