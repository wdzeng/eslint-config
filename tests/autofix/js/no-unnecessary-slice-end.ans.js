// Please also update JS/TS test if you are changing this file.
const arr = [1, 2, 3]
const sliced1 = arr.slice(1)
const sliced2 = arr.slice(1)

const str = 'abc'
const slicedStr1 = str.slice(1)
const slicedStr2 = str.slice(1)
