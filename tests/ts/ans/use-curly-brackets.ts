// Please also update JS/TS test if you are changing this file.

// Should use curly brackets ({}) for statements.
export function isOdd(n: number) {
  if (n % 2) {
    console.log(`${n} is odd.`)
  } else {
    console.log(`${n} is even.`)
  }
}
